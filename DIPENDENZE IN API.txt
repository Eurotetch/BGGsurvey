npm install react-hook-form
Dipenenza per gestire pagine

api/search.js
Proxy che Parsa XML BGG, gestisce 202, restituisce JSON

api/vercel.json
Configurazione Vercel che abilita la funzione serverless

npm install xml2js
Dipendenza per leggere xml



----------------------------------------------------------------------
USO DI XML ufficiale di Boardgamegeek:

Registrare la tua applicazione su:
👉 https://boardgamegeek.com/developer/api
Fornire:
Nome dell’app (es. “RecommendGame”)
Descrizione (es. “Personal game recommender for private use”)
Email di contatto
URL del repo (opzionale, puoi mettere GitHub)
Ottenere un User-Agent univoco, es:
1
📌 Non è una “chiave API”, ma un User-Agent identificativo da inviare in ogni richiesta.

✅ Come usarlo nel tuo proxy su Vercel.
Nel file api/search.cjs, modifica le chiamate a BGG così:
"
const BGG_USER_AGENT = 'RecommendGame/1.0 (tua.email@example.com)';

const searchRes = await fetch(
  `https://boardgamegeek.com/xmlapi2/search?query=${encodeURIComponent(q)}&type=boardgame`,
  {
    headers: {
      'User-Agent': BGG_USER_AGENT
    }
  }
);
"